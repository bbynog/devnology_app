import { ProfileStackNavigatorParamsList } from 'navigation/types';
import { NavigationProp } from '@react-navigation/native';

export const navigateToMyOrders = (
  navigation: NavigationProp<ProfileStackNavigatorParamsList>
) => {
  // navigation.navigate(StackScreen.MYORDERS);
};
